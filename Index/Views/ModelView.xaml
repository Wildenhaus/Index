<local:View x:Class="Index.Views.ModelView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:Index.Views"
            xmlns:controls="clr-namespace:Index.Controls"
            mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Menu Grid.ColumnSpan="2"
              Padding="3"
              BorderBrush="{DynamicResource Brush.Primary.3.Border.Static}"
              Background="{DynamicResource Brush.Primary.3.Background.Static}">
            <MenuItem Header="Export">
                <MenuItem Header="Export Model" Command="{Binding ExportCommand, RelativeSource={RelativeSource AncestorType={x:Type local:ModelView}}}" />
            </MenuItem>
        </Menu>

        <controls:ModelViewerControl x:Name="ModelViewer"
                                     Grid.Row="1"
                                     Model="{Binding Model}" />
    </Grid>
</local:View>
